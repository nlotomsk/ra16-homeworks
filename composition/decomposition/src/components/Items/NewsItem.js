/**
 * @description "Новости" 
 * 
 * @returns Выводит элемент списка Новостей
 * 
 * @param {string} logo - логотип СМИ
 * @param {string} subject- заголовок новости
 * @param {string} url - ссылка на новость
 */
export default function NewsItem({ logo, subject, url }) {
    return (
      <li>
      </li>
    );
  }