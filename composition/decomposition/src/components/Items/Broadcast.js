/**
 * @description "Эфир"
 * 
 * @returns Выводит элемент списка Эфиров
 * 
 * @param {string} telecast - телепрограмма
 * @param {string} channel- название эфирного канала
 * @param {string} url- ссылка на страницу вещания
 */
export default function Broadcast({ telecast, channel, url }) {
    return (
      <li>
      </li>
    );
  }