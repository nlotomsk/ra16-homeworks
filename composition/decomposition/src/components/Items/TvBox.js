/**
 * @description "Телепрограмма"
 * 
 * @returns Выводит элемент списка Телепрограммы
 * 
 * @param {string} time- время вещания
 * @param {string} channel - телепрограмма
 * @param {string} channel - название канала
 * @param {string} url - ссылка на подробную программу вещания
 * 
 */
export default function TV({ time, telecast, channel, url }) {
    return (
      <li>
      </li>
    );
  }