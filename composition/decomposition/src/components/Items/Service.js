/**
 * @description "Внутренний Сервис"
 * 
 * @returns Выводит элемент списка Внутренних Сервисов
 * 
 *  @param {string} title - название сервиса
 *  @param {string} url - ссылка на сервис
 */
export default function Service({ title, url }) {
    return (
      <li>
      </li>
    );
  }