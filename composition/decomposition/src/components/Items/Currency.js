/**
 * @description "Курс Валюты"
 * 
 * @returns Выводит элемент списка Курсов Валют
 * 
 * @param {string} currency - название валюты
 * @param {string} cost - текущий курс
 * @param {string} diff- разница курсов с предыдущим значением
 * @param {string} url - ссылка на страницу графика курса валюты
 */
export default function Currency({ currency, cost, diff, url }) {
    return (
      <li>
      </li>
    );
  }