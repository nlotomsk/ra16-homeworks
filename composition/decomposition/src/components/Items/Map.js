/**
 * @description "Карта"
 * 
 * @param {string} city - название города
 * @param {string} url - ссылку на карту
 * 
 * @returns Возврвщает карту
 */
export default function Banner({ city, url}) {
    return (
      <div>

      </div>
    );
  }