/**
 * @description Компонент "Рекламный баннер"
 * Принимает props:
 * @param {string} image - изображение баннера
 * @param {string} url - сайт рекламодателя
 * @returns Возврвщает рекламный модуль
 */
export default function Banner({ image, url }) {
    return (
      <div>

      </div>
    );
  }