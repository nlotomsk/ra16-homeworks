/**
 * @description "Погода"
 * 
 * @param {string} icon - иконка текущей погоды
 * @param {number} temperature - объект с текущей, дневной и ночной температурой
 * 
 * @returns Возвращает виджет погодв
 */
export default function Weather({ icon, temperature }) {
    return (
      <div>
      </div>
    );
  }